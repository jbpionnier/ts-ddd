{
  "name": "@jbpionnier/ddd",
  "version": "0.0.1",
  "description": "Domain-Driven Design TypeScript",
  "author": "Jean-Baptiste Pionnier",
  "license": "MIT",
  "repository": "jbpionnier/ts-ddd",
  "engines": {
    "node": ">= 8.0.0"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "lint": "tslint -p . 'src/**/*.ts'",
    "format": "prettier --write 'src/**/*.ts'",
    "test": "NODE_ENV=test jest --maxWorkers=4",
    "build": "tsc",
    "prepare": "npm run lint && npm run clean && npm run build"
  },
  "dependencies": {
    "tiny-types": "^1.12.1"
  },
  "peerDependencies": {
    "reflect-metadata": "^0.1.12"
  },
  "devDependencies": {
    "@jbpionnier/eslint-config-node": "^1.7.9",
    "@types/jest": "^24.0.11",
    "@types/node": "^12.7.2",
    "jest": "^24.7.1",
    "prettier": "^1.17.0",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^3.0.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.16.0",
    "tslint-consistent-codestyle": "^1.15.1",
    "tslint-immutable": "^6.0.1",
    "tslint-sonarts": "^1.9.0",
    "typescript": "^3.4.5"
  }
}
